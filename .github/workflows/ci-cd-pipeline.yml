name: WTF Theme CI/CD

on:
  push:
    branches: [ main, develop ]
    paths:
      - "**/*.liquid"
      - "config/**"
      - "assets/**"
      - "locales/**"
      - "templates/**"
      - "sections/**"
      - "snippets/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/wtf-theme-ci.yml"
  pull_request:
    branches: [ main ]
    paths:
      - "**/*.liquid"
      - "config/**"
      - "assets/**"
      - "locales/**"
      - "templates/**"
      - "sections/**"
      - "snippets/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/wtf-theme-ci.yml"
  workflow_dispatch:

concurrency:
  group: wtf-theme-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

env:
  SHOPIFY_STORE: wtfswag.myshopify.com
  # SHOPIFY_CLI_THEME_TOKEN: ${{ secrets.SHOPIFY_CLI_THEME_TOKEN }} # (only needed if you add deploys later)

jobs:
  changes:
    name: Detect relevant changes
    runs-on: ubuntu-latest
    outputs:
      theme: ${{ steps.filter.outputs.theme }}
      node:  ${{ steps.filter.outputs.node }}
      wf:    ${{ steps.filter.outputs.workflow }}
    steps:
      - uses: actions/checkout@v4
      - name: Paths filter
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            theme:
              - '**/*.liquid'
              - 'config/**'
              - 'assets/**'
              - 'locales/**'
              - 'templates/**'
              - 'sections/**'
              - 'snippets/**'
            node:
              - 'package.json'
              - 'package-lock.json'
            workflow:
              - '.github/workflows/wtf-theme-ci.yml'

  validate:
    name: Validate Theme (fast + reliable)
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.theme == 'true' || needs.changes.outputs.node == 'true' || github.event_name == 'workflow_dispatch' }}
    steps:
      - uses: actions/checkout@v4

      # 1) Shopify Theme Check (official; no CLI install)
      - name: Theme Check
        id: themecheck
        uses: shopify/theme-check-action@v2
        with:
          theme_root: '.'
          base: main
          token: ${{ secrets.GITHUB_TOKEN }}   # ✅ correct token context

      # 2) Node only if package.json exists
      - uses: actions/setup-node@v4
        if: hashFiles('package.json') != ''
        with:
          node-version: '20'
          cache: 'npm'

      - name: npm ci (if package.json)
        if: hashFiles('package.json') != ''
        run: npm ci

      # 3) Optional project scripts — skip if missing
      - name: conflicts:scan (if script exists)
        if: hashFiles('package.json') != ''
        run: |
          if npm run | grep -qE '(^|\s)conflicts:scan(\s|:)'; then
            npm run conflicts:scan
          else
            echo "⏭️ No conflicts:scan script; skipping"
          fi

      - name: competitors:audit (if script exists)
        if: hashFiles('package.json') != ''
        run: |
          if npm run | grep -qE '(^|\s)competitors:audit(\s|:)'; then
            npm run competitors:audit
          else
            echo "⏭️ No competitors:audit script; skipping"
          fi

      - name: order-readiness-check (if file exists)
        run: |
          if [ -f scripts/order-readiness-check.js ]; then
            node scripts/order-readiness-check.js
          else
            echo "⏭️ scripts/order-readiness-check.js not found; skipping"
          fi

      # 4) OS 2.0–aware file checks
      - name: Validate theme structure
        id: files
        shell: bash
        run: |
          set -Eeuo pipefail
          echo "Validating required theme files…"

          required=(
            "layout/theme.liquid"
            "config/settings_schema.json"
            "sections/main-product.liquid"
            "templates/product.json"
          )

          for f in "${required[@]}"; do
            if [[ ! -f "$f" ]]; then
              echo "❌ Missing required file: $f"
              exit 1
            else
              echo "✅ $f"
            fi
          done

          # Cart can be JSON (preferred) or Liquid
          if [[ -f "templates/cart.json" || -f "templates/cart.liquid" ]]; then
            echo "✅ Cart template present"
          else
            echo "❌ Missing cart template (templates/cart.json preferred or templates/cart.liquid)"
            exit 1
          fi

          # Warn if no obvious JSON-LD path for product
          if grep -R -E "schema-product|application/json[^>]*ld\+json" sections/ snippets/ 2>/dev/null; then
            echo "✅ Product schema reference detected"
          else
            echo "⚠️ No obvious Product JSON-LD found in sections/ or snippets/"
          fi

      # 5) Summaries (job + PR safe-guarded)
      - name: Write step summary
        if: always()
        shell: bash
        run: |
          {
            echo "## WTF Theme – Validation Summary"
            echo ""
            echo "- Theme Check: **${{ steps.themecheck.outcome }}**"
            echo "- Structure check: **${{ steps.files.outcome }}**"
            echo ""
            echo "_Ensure Product JSON-LD renders on product pages for SEO rich results._"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Comment summary on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const body = [
              '### ✅ WTF Theme – Validation Summary',
              '',
              `- Theme Check: **${{ steps.themecheck.outcome }}**`,
              `- Structure check: **${{ steps.files.outcome }}**`,
              '',
              '_Ensure Product JSON-LD renders on product pages for SEO rich results._'
            ].join('\n');
            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body
            });

      - name: Upload validation artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: validation-artifacts
          path: |
            docs/
            *.md
          if-no-files-found: ignore
          retention-days: 30

  security-scan:
    name: Security & Hygiene
    runs-on: ubuntu-latest
    needs: [changes, validate]
    if: ${{ (needs.changes.result == 'success' && (needs.changes.outputs.theme == 'true' || needs.changes.outputs.node == 'true')) || github.event_name == 'workflow_dispatch' }}
    steps:
      - uses: actions/checkout@v4
      - name: Scan for secrets & unsafe patterns
        shell: bash
        run: |
          set -Eeuo pipefail
          echo "🔎 Scanning for secrets…"
          FOUND=0

          if grep -R --line-number -E 'shpat_[0-9a-zA-Z]+' . --exclude-dir=.git --exclude=*.ya?ml || true; then
            echo "❌ Potential Shopify token found in repo"
            FOUND=1
          fi

          echo "🔎 Checking unsafe Liquid patterns (informational)…"
          PATTERNS=(
            '\|\s*raw'
            'assign.*request\.'
            'assign.*params\.'
          )
          for p in "${PATTERNS[@]}"; do
            if grep -R -n -E "$p" --include='*.liquid' . 2>/dev/null; then
              echo "⚠️ Possible unsafe Liquid pattern: $p"
            fi
          done

          echo "🔎 Executable bits on non-scripts (informational)…"
          find . -type f -perm /111 -not -path "./.git/*" -not -name "*.sh" -print | sed 's/^/⚠️ Exec file: /' || true

          if [[ $FOUND -ne 0 ]]; then
            exit 1
          fi
          echo "✅ Security scan complete"

  lighthouse:
    name: Lighthouse (artifact only)
    runs-on: ubuntu-latest
    needs: [changes, validate]
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.changes.outputs.theme == 'true' && needs.validate.result == 'success' }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Lighthouse CI
        run: npm i -g @lhci/cli
      - name: Run Lighthouse (placeholder)
        continue-on-error: true
        run: |
          set -euo pipefail
          mkdir -p lighthouse-results
          echo '{
            "home": {"performance": 0, "accessibility": 0, "bestPractices": 0, "seo": 0},
            "product": {"performance": 0, "accessibility": 0, "bestPractices": 0, "seo": 0}
          }' > lighthouse-results/placeholder.json
          echo "ℹ️ Configure real URLs later; storing placeholder results."
      - name: Upload Lighthouse artifacts
        uses: actions/upload-artifact@v4
        with:
          name: lighthouse-results
          path: lighthouse-results/
          retention-days: 14

  notify:
    name: CI Summary
    runs-on: ubuntu-latest
    needs: [changes, validate, security-scan, lighthouse]
    if: always()
    steps:
      - name: Print summary
        shell: bash
        run: |
          echo "# CI Summary"
          echo ""
          echo "Changes gate:"
          echo " - theme: ${{ needs.changes.outputs.theme }}"
          echo " - node : ${{ needs.changes.outputs.node }}"
          echo " - wf   : ${{ needs.changes.outputs.wf }}"
          echo ""
          echo "Validate:      ${{ needs.validate.result || 'skipped' }}"
          echo "Security-scan: ${{ needs.security-scan.result || 'skipped' }}"
          echo "Lighthouse:    ${{ needs.lighthouse.result || 'skipped' }}"
          if [ "${{ needs.validate.result }}" != "success" ] && [ "${{ needs.validate.result }}" != "skipped" ]; then
            echo "🔴 Not ready — fix validation failures."
            exit 1
          elif [ "${{ needs.security-scan.result }}" = "failure" ]; then
            echo "🔴 Not ready — fix security failures."
            exit 1
          else
            echo "🟢 Looks good (or no relevant changes)."
          fi

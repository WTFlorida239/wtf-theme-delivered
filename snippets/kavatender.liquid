<div id="kavatender-root" class="kava-hidden" aria-live="polite">
  <button id="kavatender-launcher" aria-label="Open Virtual Kavatender">
    💬 Kavatender
  </button>

  <div id="kavatender-panel" role="dialog" aria-modal="true" aria-labelledby="kavatender-title">
    <div class="kava-header">
      <div>
        <h2 id="kavatender-title">Virtual Kavatender</h2>
        <p class="kava-sub">21+ • Not medical advice</p>
      </div>
      <button id="kavatender-close" aria-label="Close">✕</button>
    </div>
    <div id="kavatender-messages" class="kava-messages" tabindex="0"></div>

    <form id="kavatender-form" autocomplete="off">
      <input id="kavatender-input" name="message" placeholder="Tell me how you want to feel…" required />
      <button id="kavatender-send" type="submit">Send</button>
    </form>
  </div>
</div>

<style>
  #kavatender-launcher {
    position: fixed; right: 16px; bottom: 16px; z-index: 50;
    border: 0; border-radius: 999px; padding: 12px 16px; box-shadow: 0 8px 20px rgba(0,0,0,.2);
    background: #111; color: #fff; font-weight: 600; cursor: pointer;
  }
  #kavatender-panel {
    position: fixed; right: 16px; bottom: 76px; width: 360px; max-width: 95vw; height: 520px;
    background: #fff; border-radius: 14px; box-shadow: 0 20px 40px rgba(0,0,0,.25); display: none; z-index: 50;
    overflow: hidden; display: grid; grid-template-rows: auto 1fr auto;
  }
  .kava-header { display:flex; justify-content:space-between; align-items:center; padding:12px 14px; border-bottom:1px solid #eee; background:#fafafa; }
  .kava-sub { font-size: 12px; color:#666; margin: 2px 0 0; }
  .kava-messages { padding:12px; overflow:auto; }
  .kava-msg { margin:10px 0; line-height:1.45; }
  .kava-msg.user { text-align:right; }
  .kava-card { border:1px solid #eee; border-radius:10px; padding:10px; margin-top:8px; }
  #kavatender-form { display:flex; gap:8px; padding:10px; border-top:1px solid #eee; background:#fff; }
  #kavatender-input { flex:1; padding:10px; border-radius:10px; border:1px solid #ddd; }
  #kavatender-root.kava-hidden { opacity:0 } /* initial to avoid CLS */
</style>
